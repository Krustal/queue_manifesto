<%= form_for([@task_queue, @task_node]) do |f| %>
  <% if @task_node.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_node.errors.count, "error") %> prohibited this task_node from being saved:</h2>

      <ul>
      <% @task_node.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
