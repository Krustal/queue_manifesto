<%= form_for(@task_queue) do |f| %>
  <% if @task_queue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_queue.errors.count, "error") %> prohibited this task_queue from being saved:</h2>

      <ul>
      <% @task_queue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
